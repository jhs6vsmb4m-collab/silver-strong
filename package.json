{
  "name": "silver-strong",
  "version": "0.1.0",
  "private": true,
  "description": "A mobile-first fitness app for adults 55+ focused on safe seated and low impact exercises.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "build": "rm -rf dist && mkdir dist && cp index.html styles.css app.js dist/"
  }
}
